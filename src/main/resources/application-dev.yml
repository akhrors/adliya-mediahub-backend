spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/adliya_mediahub
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

logging:
  level:
    uz.adevs.mediahub: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
